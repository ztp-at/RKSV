Actually check if a device even has a camera before displaying the button
currently serials without [A-F] will be interpreted as decimal, should be hex and then allow fallback to decimal for old stuff
investigate UnicodeDecodeErrors
improve UI, make long text in text fields wrap, not scroll, add hint texts to tfs...
Find a way around the rounding errors in the turnover counter
Write a test suite for the verification functions, see if a-sit tests can help here
Support both counter update methods described in new doc
